apiVersion: v3.juiceshop.helmetica-bundles.io/bundle
kind: Enum
metadata:
  annotations:
    match-error: 'Unsupported value: "invalid": supported values: "a", "b", "c"'
  name: enums
spec:
  values:
    stringmap:
      a: invalid


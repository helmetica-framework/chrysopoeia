---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: instancerevisions.helmetica.io
spec:
  group: helmetica.io
  names:
    kind: InstanceRevision
    listKind: InstanceRevisionList
    plural: instancerevisions
    singular: instancerevision
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: InstanceRevision is the Schema for the bundlesources API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstanceRevisionSpec defines the desired state of InstanceRevision.
            properties:
              values:
                description: Values is the values of the Helm chart.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              version:
                description: Version is the version of the Helm chart.
                minLength: 1
                type: string
            required:
            - values
            - version
            type: object
          status:
            description: InstanceRevisionStatus defines the observed state of InstanceRevision
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

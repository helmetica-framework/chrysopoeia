---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: bundlesources.helmetica.io
spec:
  group: helmetica.io
  names:
    kind: BundleSource
    listKind: BundleSourceList
    plural: bundlesources
    singular: bundlesource
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BundleSource is the Schema for the bundlesources API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BundleSourceSpec defines the desired state of BundleSource.
            properties:
              majorVersions:
                description: MajorVersions defines the major versions of the bundles
                  that should be made available.
                items:
                  type: integer
                type: array
              ociURI:
                description: OCIURI is the URI of the OCI registry where the bundles
                  are stored.
                type: string
            required:
            - majorVersions
            - ociURI
            type: object
        type: object
    served: true
    storage: true

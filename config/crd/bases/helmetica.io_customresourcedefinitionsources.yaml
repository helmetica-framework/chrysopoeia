---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: customresourcedefinitionsources.helmetica.io
spec:
  group: helmetica.io
  names:
    kind: CustomResourceDefinitionSource
    listKind: CustomResourceDefinitionSourceList
    plural: customresourcedefinitionsources
    singular: customresourcedefinitionsource
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CustomResourceDefinitionSource is the Schema for the bundlesources
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CustomResourceDefinitionSourceSpec defines the desired state
              of CustomResourceDefinitionSource.
            properties:
              reference:
                description: Reference is a reference to the source of the bundle.
                properties:
                  apiVersion:
                    description: APIVersion of the referent.
                    enum:
                    - source.toolkit.fluxcd.io/v1
                    type: string
                  kind:
                    description: Kind of the referent.
                    enum:
                    - OCIRepository
                    type: string
                  name:
                    description: Name of the referent.
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - reference
            type: object
        type: object
    served: true
    storage: true
